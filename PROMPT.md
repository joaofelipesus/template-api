- configure devise and auth using JWT
- add auth verification
- add a rake task to create users
- associate user and book (user_book)
- add routes
1. GET /user_books
2. POST /user_books {book_id}
3. GET books/search?title=....
